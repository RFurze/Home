# Reproducible environment for mybinder.org (legacy FEniCS 2019.1.0).
# Colab does NOT use this — the notebook installs FEniCS itself via fem-on-colab.
name: hmm-demo
channels:
  - conda-forge
dependencies:
  - python=3.10
  - fenics=2019.1.0
  - numpy
  - matplotlib
  - jupyterlab
